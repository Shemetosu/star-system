spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/star_system
    username: ${USERNAME}
    password: ${PASSWORD}
  jpa:
    open-in-view: false